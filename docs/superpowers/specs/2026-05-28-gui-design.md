# Apple Books Exporter GUI 设计文档

## 概述

为 Rust 版本的 Apple Books Exporter 构建桌面 GUI 应用，使用 Tauri v2 + Svelte 技术栈，macOS 原生风格。

## 技术栈

- **后端**: Tauri v2 (Rust)，复用现有 db.rs / exporter.rs / provider.rs 等模块
- **前端**: Svelte + Vite
- **样式**: macOS vibrancy 毛玻璃，原生标题栏，系统暗色/亮色自动适配

## 启动流程与权限引导

Apple Books 数据库位于 `~/Library/Containers/com.apple.iBooksX/Data/Documents/`，需要"完全磁盘访问权限"。

```
启动 App
    │
    ▼
检测数据库是否可访问
    │
    ├── 可访问 → 正常加载书籍列表
    │
    └── 不可访问 → 显示引导弹窗
                    │
                    ├── 说明: "需要授予完全磁盘访问权限"
                    ├── 显示路径: ~/Library/Containers/com.apple.iBooksX/...
                    ├── 操作步骤:
                    │   1. 打开 系统设置 → 隐私与安全性 → 完全磁盘访问权限
                    │   2. 添加 Apple Books Exporter
                    │   3. 重启 App
                    ├── [打开系统设置] 按钮 (调用 macOS URL Scheme)
                    └── [重新检测] 按钮
```

## 配置持久化

| 配置项 | 存储位置 | 说明 |
|--------|----------|------|
| API Key | macOS Keychain | 敏感信息，不落盘到文件 |
| Base URL / Model / Provider | `~/Library/Application Support/books-exporter/config.json` | Tauri app data 目录 |
| 默认输出目录 / 格式 | 同上 | 用户偏好 |
| 书籍缓存 | `~/Library/Application Support/books-exporter/cache.json` | LLM 缓存 |

## 架构

```
┌─────────────────────────────────────────────────────┐
│                  Tauri v2 (Rust)                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │ commands │  │  state   │  │   DB / Cache /    │   │
│  │ (invoke) │  │ (managed)│  │   Provider        │   │
│  └────┬─────┘  └──────────┘  └──────────────────┘   │
│       │ Tauri IPC                                     │
├───────┼───────────────────────────────────────────────┤
│       │ Svelte 前端                                    │
│  ┌────▼──────────────────────────────────────────┐   │
│  │  SidebarNav  ←→  Page Components              │   │
│  │  ┌────────┐    ┌─────────┐ ┌──────────┐       │   │
│  │  │ Books  │    │ Export  │ │ Enrich   │       │   │
│  │  │ Card   │    │ Card    │ │ Cache    │       │   │
│  │  │ Config │    │ Preview │ │ Settings │       │   │
│  │  └────────┘    └─────────┘ └──────────┘       │   │
│  └────────────────────────────────────────────────┘   │
│           WebKit 渲染 + vibrancy 毛玻璃               │
└─────────────────────────────────────────────────────┘
```

- 前端只负责 UI 渲染和交互，业务逻辑全在 Rust 侧
- DB 连接作为 Tauri managed state，避免重复打开

## 布局

单窗口 + 侧边栏导航：

```
┌──────────────────────────────────────────────────────────────┐
│  [─][□][×]          Apple Books Exporter            (毛玻璃)  │
├────────┬─────────────────────────────────────────────────────┤
│  📚    │                                                     │
│  📤    │   当前页面内容区                                      │
│  ✨    │   (列表/详情/配置/预览)                                │
│  🎨    │                                                     │
│  💾    │                                                     │
│  ⚙️    │                                                     │
│        │                                                     │
├────────┴─────────────────────────────────────────────────────┤
│  📚 12 本书  │  💾 缓存 45 条  │  🕐 最后同步 10:30  │ v0.2.0 │
└──────────────────────────────────────────────────────────────┘
```

## 页面设计

### 1. 书籍列表页 (Books)

```
┌──────────────────────────────────────────────┐
│  🔍 搜索书籍...                    [刷新]     │
├──────────────────────────────────────────────┤
│  ┌────────────────────────────────────────┐  │
│  │ 📖 深入理解计算机系统        5 笔记   │  │
│  │    Randal E. Bryant                    │  │
│  ├────────────────────────────────────────┤  │
│  │ 📖 Python编程：从入门到实践  12 笔记  │  │
│  │    Eric Matthes                        │  │
│  ├────────────────────────────────────────┤  │
│  │ 📖 算法导论                  8 笔记   │  │
│  │    Thomas H. Cormen                    │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  ◄  第 1/3 页  ►                             │
└──────────────────────────────────────────────┘
```

- 卡片式列表，显示书名、作者、笔记数
- 支持搜索过滤
- 点击进入详情

### 2. 导出页 (Export)

```
┌──────────────────────────────────────────────┐
│  选择书籍: [深入理解计算机系统      ▼]       │
│                                              │
│  输出格式: ● Obsidian  ○ Markdown            │
│  输出目录: [~/Documents/books/    ] [选择]    │
│                                              │
│  ┌────────────────────────────────────────┐  │
│  │ 预览:                                  │  │
│  │ ---                                    │  │
│  │ type: llm-note                         │  │
│  │ book: 深入理解计算机系统               │  │
│  │ ---                                    │  │
│  │ ## 高亮与标注                          │  │
│  │ ...                                    │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  [导出 Markdown]                    进度: 0% │
└──────────────────────────────────────────────┘
```

### 3. AI 增强页 (Enrich)

```
┌──────────────────────────────────────────────┐
│  书籍: [深入理解计算机系统          ▼]       │
│  模式: ● 增量  ○ 全量  ○ 单条 [序号: ___]    │
│                                              │
│  ┌────────────────────────────────────────┐  │
│  │ 处理队列:                              │  │
│  │ ✅ #1 "计算机系统概述..." → 已生成     │  │
│  │ ✅ #2 "存储器层次结构..." → 已生成     │  │
│  │ 🔄 #3 "程序的机器级表示..." → 处理中   │  │
│  │ ⏳ #4 ...                              │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  [开始处理]  [强制刷新]        已缓存: 2/5 (40%) │
└──────────────────────────────────────────────┘
```

### 4. 卡片生成页 (Card)

```
┌──────────────────────────────────────────────┐
│  书籍: [深入理解计算机系统          ▼]       │
│  样式: ● Dark  ○ Light  ○ Minimal  (自定义模板: 后续迭代) │
│                                              │
│  ┌────────────────────────────────────────┐  │
│  │  ┌──────────────────────────────────┐  │  │
│  │  │  "计算机系统是由硬件和软件..."   │  │  │
│  │  │                                  │  │  │
│  │  │  深入理解计算机系统              │  │  │
│  │  └──────────────────────────────────┘  │  │
│  │  卡片预览                              │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  输出目录: [~/cards/              ] [选择]    │
│  [生成全部]  [生成选中]                       │
└──────────────────────────────────────────────┘
```

### 5. 缓存页 (Cache)

```
┌──────────────────────────────────────────────┐
│  书籍: [深入理解计算机系统          ▼]       │
│                                              │
│  缓存统计:                                   │
│  ┌────────────────────────────────────────┐  │
│  │ 总笔记: 5                              │  │
│  │ 已缓存: 3 (60%)                        │  │
│  │ 未缓存: 2                              │  │
│  │                                        │  │
│  │ 缓存条目:                              │  │
│  │ • #1 "计算机系统概述" - 2024-01-15    │  │
│  │ • #2 "存储器层次结构" - 2024-01-15    │  │
│  │ • #3 "程序的机器级表示" - 2024-01-16  │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  [清空缓存]                                  │
└──────────────────────────────────────────────┘
```

### 6. 配置页 (Config)

```
┌──────────────────────────────────────────────┐
│  LLM 配置:                                   │
│  Provider: [OpenAI Compatible      ▼]        │
│            (OpenAI / OpenAI Compatible / 自定义) │
│  Base URL: [https://api.example.com/v1    ]  │
│  API Key:  [••••••••••••••••            ]     │
│  Model:    [mimo-v2.5-pro               ]    │
│                                              │
│  输出配置:                                   │
│  默认格式: [Obsidian                ▼]       │
│  输出目录: [~/Documents/books/    ] [选择]    │
│                                              │
│  [保存配置]  [测试连接]                       │
└──────────────────────────────────────────────┘
```

## Tauri Commands (IPC)

| Command | 功能 | 返回 |
|---------|------|------|
| `check_db_access` | 检测数据库是否可访问 | `bool` |
| `open_system_settings` | 打开系统设置(权限页) | `void` |
| `get_books` | 获取书籍列表 | `Vec<Book>` |
| `get_annotations` | 获取某书笔记 | `Vec<Annotation>` |
| `export_book` | 导出 Markdown | 进度事件 + 文件路径 |
| `enrich_book` | AI 增强 | 进度事件 + 结果 |
| `generate_cards` | 生成卡片 | 进度事件 + 文件路径 |
| `get_cache_stats` | 缓存统计 | `CacheStats` |
| `clear_cache` | 清空缓存 | `bool` |
| `load_config` | 加载配置 | `Config` |
| `save_config` | 保存配置 | `bool` |
| `test_connection` | 测试 LLM 连接 | `bool` |

## 事件类型

Rust 通过 Tauri event 机制向前端推送三种事件：

**进度事件：**
```
event: "progress"
data: { "current": 3, "total": 5, "status": "processing", "message": "处理第3条笔记..." }
```

**错误事件：**
```
event: "error"
data: { "message": "处理失败：网络超时" }
```

**完成事件：**
```
event: "complete"
data: { "success": 5, "failed": 0 }
```

前端监听事件更新进度条和状态，错误事件触发 toast 通知。

## 错误处理

- **Rust 侧**: 所有 command 返回 `Result<T, String>`，错误信息中文
- **前端**: 统一 toast 通知，不阻断操作
- **数据库不存在**: 启动时检测，显示引导弹窗（见"启动流程与权限引导"）
- **LLM 连接失败**: 配置页"测试连接"按钮即时反馈，enrich 页失败时 toast 提示

## 项目结构

```
books-exporter/
├── src-tauri/                 # Tauri Rust 后端
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   ├── src/
│   │   ├── main.rs            # Tauri 入口
│   │   ├── commands.rs        # 所有 Tauri commands
│   │   ├── state.rs           # 管理状态 (DB连接等)
│   │   └── lib.rs             # 复用现有模块
│   └── icons/
├── src/                       # Svelte 前端
│   ├── App.svelte
│   ├── lib/
│   │   ├── Sidebar.svelte
│   │   ├── pages/
│   │   │   ├── Books.svelte
│   │   │   ├── Export.svelte
│   │   │   ├── Enrich.svelte
│   │   │   ├── Card.svelte
│   │   │   ├── Cache.svelte
│   │   │   └── Config.svelte
│   │   └── components/
│   │       ├── BookCard.svelte
│   │       ├── ProgressBar.svelte
│   │       ├── Toast.svelte
│   │       ├── ConfirmDialog.svelte
│   │       └── LoadingSpinner.svelte
│   └── app.css                # macOS vibrancy 样式
├── package.json
└── vite.config.ts
```

## macOS 原生特性

- `tauri-plugin-vibrancy` — 毛玻璃背景
- 原生标题栏 + 交通灯按钮
- 系统暗色/亮色模式自动适配
- SF Symbols 风格图标

## 依赖

- Tauri v2 + Svelte + Vite
- `@tauri-apps/api` — IPC 调用
- 现有 Rust 模块全部复用，不改代码结构

## 实现顺序

1. 项目初始化：Tauri + Svelte 脚手架，vibrancy 配置
2. 侧边栏导航 + 页面路由
3. 书籍列表页 (Books)
4. 导出页 (Export)
5. 配置页 (Config)
6. AI 增强页 (Enrich)
7. 卡片生成页 (Card)
8. 缓存页 (Cache)
9. 错误处理 + toast 通知
10. 打包测试
