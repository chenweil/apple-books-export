# Books Exporter v0.1.8

状态：发布候选（unsigned，build 9）。

## Share Card 编辑器

- 选中 Apple Books 标注后可从详情行进入编辑器，默认立即生成可预览卡片。
- 支持自动/固定字号、水平/垂直对齐、11 款随应用分发字体和 12 个完整模板。
- 长正文、长笔记和中英文混排形成连续的 1200×1600 PNG 页面，不截断内容。
- 大预览、当前页页码和有边界缩略图带共享同一组分页与渲染结果。
- 保存全部页面；复制默认当前页并提供复制全部页面；AirDrop 发送当前页临时 PNG。
- 移除泛用 macOS 分享面板；编辑卡片不会改写原始 Apple Books 标注。

## 验证

服务层测试、AppKit UI 探针和代表性生成图片的执行结果记录在
[`docs/plans/2026-08-05-appkit-share-card-issue-13-verification.md`](../plans/2026-08-05-appkit-share-card-issue-13-verification.md)。

本地 unsigned DMG：

```bash
cd appkit
./Scripts/package-dmg.sh
```

产物为 `dist/Books-Exporter-0.1.8-unsigned.dmg`。该产物未签名、未 notarization，
只适用于本机安装和构建验证。

## 发布前限制

- 当前会话未在两台真实设备之间执行 AirDrop 发送；自动化仅验证当前页临时 PNG 的生成条件和入口状态。
- 当前提交不创建 GitHub Release，也不执行远程推送；发布时需使用已验证 DMG 并补录 Release URL。
