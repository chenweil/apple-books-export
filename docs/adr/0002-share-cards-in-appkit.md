# Share cards live in the AppKit application

The current Apple Books exporter experience is the AppKit application, so Share Cards will be implemented there. We will not extend or reuse the older card interface from another product version, avoiding a split feature surface and keeping future maintenance with the version readers actually use.
