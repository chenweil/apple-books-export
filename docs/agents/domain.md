# Domain Docs

This is a single-context repository. Before exploring or changing the code, read the root `CONTEXT.md` and relevant records under `docs/adr/`.

Use terms from `CONTEXT.md` in specifications, tickets, code, and tests. Surface conflicts with applicable ADRs rather than silently overriding them.
